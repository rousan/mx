import DefaultTheme from 'vitepress/theme';
import './custom.css';

/**
 * The docs theme is VitePress's default theme with a thin CSS layer
 * (`custom.css`) that repaints its brand tokens to the teal-accent, calm-
 * monochrome palette used by the mx landing site, so `/docs` reads as the same
 * product rather than a default-blue VitePress site.
 */
export default DefaultTheme;
